export const loginUser = user => ({
  type: 'LOGIN_USER',
  user
});

export const logoutUser = user => ({
  type: 'LOGOUT_USER',
  user
});

export const registerUser = user => ({
  type: 'REGISTER_USER',
  user
});

